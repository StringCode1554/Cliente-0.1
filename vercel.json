{
  "buildCommand": "pnpm --filter @workspace/aora-cucina run build",
  "outputDirectory": "artifacts/aora-cucina/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "BASE_PATH": "/"
  }
}
